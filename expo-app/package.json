{
  "name": "solanapod-expo",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "build:apk": "npx eas build -p android --profile production",
    "build:apk:clean": "npx eas build -p android --profile production --clear-cache",
    "build:preview": "npx eas build -p android --profile preview",
    "postinstall": "patch-package"
  },
  "dependencies": {
    "expo": "~54.0.0",
    "expo-audio": "^55.0.8",
    "expo-device": "~7.0.3",
    "expo-foreground-actions": "^0.4.5",
    "expo-splash-screen": "~31.0.13",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "react-native-webview": "13.15.0"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "babel-preset-expo": "~54.0.10",
    "eas-cli": "^14.0.0",
    "patch-package": "^8.0.1"
  }
}
